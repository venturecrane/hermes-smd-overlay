__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
*.egg
.eggs/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.venv/
venv/
.env
.env.*
!.env.example
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~
.mypy_cache/
.tox/
node_modules/

# test artifacts (D1Client default binding writes a local sqlite file)
CUSTOMER_DB
*.db

# Local git worktrees (isolated per-task checkouts) must never be tracked —
# a committed gitlink with no .gitmodules breaks `uv pip install git+...` (the
# seat build). ss-console#1806 reprovision hit exactly this.
.worktrees/
