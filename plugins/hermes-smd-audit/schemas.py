"""Audit row shape + accepted action_type enum.

Stub. §7 ports from ss-console/ai-employee/adapter/audit_log.py ACCEPTED_ACTION_TYPES.
GEPA-related actions (per ADR 0018 superseded) are NOT carried forward.
"""

# Placeholder. Replaced by the ported AuditRow dataclass + ACTION_TYPES set.
ACTION_TYPES: set[str] = set()
